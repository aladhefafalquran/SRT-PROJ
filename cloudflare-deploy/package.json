{
  "name": "srt-studio",
  "version": "1.0.0",
  "private": true,
  "description": "SRT Studio — browser-based subtitle editor and renderer. Deployed on Cloudflare Pages.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name srt-studio --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
